package com.evilbird.warcraft.action.type;

import com.evilbird.engine.action.ActionIdentifier;

public enum CommonAction implements ActionIdentifier
{
    Select,
    Move,
    Attack,
    Stop,
    Cancel,
    Repair,
    Reposition
}
